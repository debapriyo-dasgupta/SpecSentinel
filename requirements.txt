# SpecSentinel — Python dependencies
# Install with: pip install -r requirements.txt

# Vector database
chromadb>=0.5.0

# Web scraping for rule ingestion
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# YAML parsing for OpenAPI specs
pyyaml>=6.0

# API framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9    # for file uploads

# Scheduling
APScheduler>=3.10.0

# Utilities
python-dateutil>=2.9.0

# AI Agents (Optional - for enhanced analysis)
# Install at least one for AI-powered insights:
openai>=1.12.0             # OpenAI GPT models (gpt-4o-mini, gpt-4o)
anthropic>=0.18.0          # Anthropic Claude models (claude-3-5-sonnet, claude-3-haiku)
google-generativeai>=0.3.0 # Google Gemini models (gemini-1.5-pro, gemini-1.5-flash)
